global:
  resolve_timeout: 5m

route:
  receiver: webhook-test
  group_by: [alertname]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 2h

receivers:
  - name: webhook-test
    webhook_configs:
      - url: http://alert-sink:8080/webhook
        send_resolved: true
