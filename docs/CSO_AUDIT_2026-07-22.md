# CSO daily audit — Medora (2026-07-22)

Mode: daily (8/10 confidence gate). Scope: specialty persistence + selling surfaces + auth.

## Architecture snapshot

- TanStack Start / Cloudflare Workers  
- Supabase Postgres + RLS + service-role server routes  
- Demo auth (sessionStorage) gated off in production builds  

## Findings (post-remediation)

### Remediatied this pass

| ID | Severity | Confidence | Issue | Fix |
|----|----------|------------|-------|-----|
| CSO-1 | P0 | 9/10 | `/api/hospital/persist` same-origin + service role = unauthenticated PHI R/W + tenant create | `hospital-persist-auth.ts`: JWT membership scope, demo header non-prod only, API key for ops; ignore client `hospital_id` |
| CSO-2 | P0 | 9/10 | Onboard auto-provisioned hospitals for anyone | Lead-only by default; provision requires `canProvision`; IP rate limit |
| CSO-3 | P1 | 9/10 | RLS `onboarding_insert_anon` WITH CHECK (true) | Dropped; staff insert policy only (service role for API) |
| CSO-4 | P2 | 8/10 | Client-only license key | Added `src/server/license.ts` (`MEDORA_LICENSE_KEY`) for server entitlement checks |

### Accepted / deferred (below ship bar for this PR)

| ID | Severity | Confidence | Issue | Next |
|----|----------|------------|-------|------|
| CSO-5 | P2 | 8/10 | Demo persist header usable if `ALLOW_DEMO_PERSIST` mis-set in prod | Document in PRODUCTION.md; default off in production runtime |
| CSO-6 | P2 | 8/10 | No PHI access audit export yet | SELLING_READY 3.3 |
| CSO-7 | P3 | 8/10 | In-memory onboard rate limit is per-isolate on Workers | Add Turnstile / Durable Object counter |
| CSO-8 | P3 | 9/10 | Evaluation watermark spoofable in browser | Expected; server license is source of truth |

## STRIDE (persist API)

| Threat | Status |
|--------|--------|
| Spoofing | Mitigated: JWT / API key / gated demo |
| Tampering | Mitigated: hospital_id forced server-side |
| Repudiation | Partial: AI audit exists; clinical persist audit TBD |
| Information disclosure | Mitigated for cross-tenant; demo still Oak Haven only |
| Denial of service | Partial: onboard rate limit |
| Elevation of privilege | Mitigated: provision requires admin/api key |

## Verdict

**Ship-eligible for evaluation / pilot** after this auth harden.  
**Not yet** “HIPAA production for paying hospitals” until CSO-6 + BAA + Stripe.
